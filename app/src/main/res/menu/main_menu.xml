<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- File Menu -->
    <item
        android:id="@+id/action_new"
        android:title="New"
        android:icon="@android:drawable/ic_menu_add"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_create_sample"
        android:title="Create Sample File"
        android:showAsAction="never" />

    <item
        android:id="@+id/action_open"
        android:title="Open"
        android:icon="@android:drawable/ic_menu_upload"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_save"
        android:title="Save"
        android:icon="@android:drawable/ic_menu_save"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_save_as"
        android:title="Save As..."
        app:showAsAction="never" />

    <!-- Edit Menu -->
    <group android:id="@+id/group_edit">
        <item
            android:id="@+id/action_undo"
            android:title="Undo"
            android:icon="@android:drawable/ic_menu_revert"
            app:showAsAction="never" />

        <item
            android:id="@+id/action_redo"
            android:title="Redo"
            app:showAsAction="never" />

        <item
            android:id="@+id/action_cut"
            android:title="Cut"
            app:showAsAction="never" />

        <item
            android:id="@+id/action_copy"
            android:title="Copy"
            app:showAsAction="never" />

        <item
            android:id="@+id/action_paste"
            android:title="Paste"
            app:showAsAction="never" />
    </group>

    <item
        android:id="@+id/action_run"
        android:title="Run"
        android:icon="@drawable/ic_play" />

    <item
        android:id="@+id/action_compile_run"
        android:title="Compile and Run"
        android:icon="@drawable/ic_compile_run" />

    <!-- Settings Menu -->
    <item
        android:id="@+id/action_syntax_config"
        android:title="Syntax Configuration"
        android:icon="@android:drawable/ic_menu_preferences"
        app:showAsAction="never" />

</menu>